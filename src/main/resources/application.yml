spring:
  application:
    name: docextract
  mvc:
    static-path-pattern: /ui/**

server:
  servlet:
    context-path: /adeon-docextract

---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: ci
---
spring:
  config:
    activate:
      on-profile: cluster
